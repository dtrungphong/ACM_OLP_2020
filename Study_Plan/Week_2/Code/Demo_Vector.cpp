#include <bits/stdc++.h>
using namespace std;

int main(){
	vector <int> p;
	int n; cin>>n;
	p.push_back(1); // add elements
	p.push_back(2); // add elements
	p.push_back(3); // add elements
	p.push_back(4); // add elements
	p.size() // return size of vector p
	sort(p.begin(),p.end());
	return 0;
}